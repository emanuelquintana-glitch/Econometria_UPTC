#+TITLE: Econometria ejercicio 13.8 Damodar Gujarati
#+AUTHOR: Emanuel Quintana Silva
#+DATE: <2026-05-20 Wed>

** Enunciado: Segun la hipotesis de ingreso permanente de Friedman, podemos escribir

$$Y_{i}^{*} = \alpha + \beta X_{i}^{}$$

donde $Y_{i}^{*}$ = gasto de consumo ""permanente" y $X_{i}$ = ingreso "permanente". EN lugar de las variables "permanentes", observamos

$$Y_{i} = Y_{i}^{*} + u_{i}$$
$$X_{i} = X_{i}^{* }+ v_{i}$$

donde $Y_{i}$ y $X_{i}$ son las cantidades observables o mesurables, y donde $u_{i}$ y $v_{i}$ son los errores de medicion en $Y^{*}^{*}$ y $X^{*}$, respectivamente.
Con las cantidades observables, escribimos la funcion de consumo como:

$$Y_{i} = \alpha + \beta(X_{i} - v_{i}) + u_{i}$$
$$ = \alpha + \beta X_{i} + (u_{i }- \beta v_{i})$$

Si suponemos que 1) $E(u_{i})= E(v_{i})=0$, 2) $var(u_{i}) = \sigma_{u}^{2}$ y $var(v_{i})= \sigma_{v}^{2}$, 3) $cov(Y_{i}_{}^{*},u_{i}) = 0$, 4) $cov(u_{i}, X_{i}^{*}) = cov(v_{i},Y_{i}^{*}) = cov(u_{i},v_{i}) = 0$, demuestre que, en muestras grandes, el \beta estimado de (2) se expresa como


$$plim(\hat{\beta}) = \frac{\beta}{1 + (\frac{\sigma_{v}^{2}}{\sigma_{x}^{2}})}$$

a) ¿Que puede decir sobre la naturaleza del sesgo en $\hat{\beta}?$

b) Si el tamaño de la muestra aumenta indefinidamente, ¿tendera el $\beta$ estimado a igualar ek  \beta verdadero?

** Solucion
